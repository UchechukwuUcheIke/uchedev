import { Metadata } from 'next'
import "./page.css"
import {Button, Typography, Stack, Paper, Card, CardHeader, CardMedia, CardContent, CardActionArea} from '@mui/material';
import Link from "next/link";
import ContentCard from '../ContentCard';

//TODO: Not very useful rn, but will become more important when we switch to storing articles on a CMS
export default function Loading() {
    <p> Loading </p>
}